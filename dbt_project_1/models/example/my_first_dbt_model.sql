create or replace table `my-data-engineer-project`.`dbt_project`.`my_first_dbt_model`
      
    
    

    OPTIONS()
    as (
      create or replace table `my-data-engineer-project`.`dbt_project`.`my_first_dbt_model`
      
    
    

    OPTIONS()
    as (
      with source_data as (

    select 1 as id
    union all
    select null as id

)

select *
from source_data
    );
    );